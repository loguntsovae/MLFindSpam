# 🚀 Быстрое обновление на сервере

## Ситуация: контейнер уже запущен, нужно обновить

### ❌ Старый способ (ошибка):
```bash
git pull
make docker-build
make docker-run  # ← Ошибка: контейнер уже существует
```

### ✅ НОВЫЙ способ (автоматически):

#### Вариант 1: Использовать `make docker-restart`
```bash
git pull
make docker-restart
```

Это автоматически:
1. Остановит старый контейнер
2. Удалит старый контейнер
3. Пересоберёт образ
4. Запустит новый контейнер

#### Вариант 2: Использовать docker-compose (рекомендуется)
```bash
git pull
make docker-compose-rebuild
```

Это автоматически:
1. Остановит все сервисы
2. Пересоберёт образы
3. Запустит новые контейнеры

#### Вариант 3: Вручную (пошагово)
```bash
git pull
make docker-stop     # Остановить и удалить
make docker-build    # Пересобрать
make docker-run      # Запустить
```

## 📋 Все новые команды

```bash
# Одной командой - пересобрать и перезапустить
make docker-restart

# Docker Compose - пересобрать и перезапустить
make docker-compose-rebuild

# Проверить статус
docker ps
docker logs spam_classifier

# Остановить контейнер
make docker-stop

# Или через docker-compose
docker-compose down
```

## 🔄 Типичный workflow обновления

### На локальной машине:
```bash
# Внести изменения
git add .
git commit -m "Update UI with multilingual examples"
git push
```

### На сервере Ubuntu:
```bash
# Получить изменения и обновить
cd ~/app/MLFindSpam
git pull
make docker-restart

# Проверить что работает
curl http://localhost:5001
```

## 🎯 Быстрые команды

```bash
# Полное обновление (одна команда)
git pull && make docker-restart

# С docker-compose
git pull && make docker-compose-rebuild

# Проверка логов
docker logs -f spam_classifier

# Проверка статуса
docker ps | grep spam_classifier
```

## 🐛 Устранение проблем

### Проблема: Контейнер не останавливается
```bash
docker ps -a | grep spam_classifier
docker rm -f spam_classifier
make docker-build
make docker-run
```

### Проблема: Порт уже занят
```bash
# Найти процесс на порту 5001
lsof -i :5001
# или
netstat -tlnp | grep 5001

# Убить процесс
kill -9 <PID>

# Перезапустить
make docker-restart
```

### Проблема: Старый образ остался
```bash
# Удалить все остановленные контейнеры
docker container prune -f

# Удалить неиспользуемые образы
docker image prune -a -f

# Пересобрать
make docker-build
make docker-run
```

## 📊 Проверка обновления

После обновления проверьте:

```bash
# 1. Контейнер запущен
docker ps | grep spam_classifier

# 2. Логи без ошибок
docker logs spam_classifier | tail -20

# 3. API работает
curl http://localhost:5001

# 4. Предсказание работает (английский)
curl -X POST http://localhost:5001/predict \
  -H "Content-Type: application/json" \
  -d '{"message":"Win free iPhone now!"}'

# 5. Предсказание работает (русский)
curl -X POST http://localhost:5001/predict \
  -H "Content-Type: application/json" \
  -d '{"message":"СРОЧНО! Вы выиграли iPhone!"}'
```

## ✅ Готово!

Теперь можно быстро обновлять проект одной командой:
```bash
git pull && make docker-restart
```

## 📝 Шпаргалка команд

| Задача | Команда |
|--------|---------|
| Обновить код и контейнер | `git pull && make docker-restart` |
| Остановить контейнер | `make docker-stop` |
| Пересобрать образ | `make docker-build` |
| Запустить контейнер | `make docker-run` |
| Все в одном | `make docker-restart` |
| С docker-compose | `make docker-compose-rebuild` |
| Посмотреть логи | `docker logs -f spam_classifier` |
| Проверить статус | `docker ps` |

---

**Совет**: Используйте `make docker-restart` или `make docker-compose-rebuild` для быстрого обновления! 🚀
