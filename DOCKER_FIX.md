# 🐳 Docker Fix - Исправление ошибки монтирования

## ❌ Проблема
```
Error response from daemon: failed to create task for container: 
error mounting "/home/ubuntu/app/MLFindSpam/model.pkl" to rootfs at "/app/model.pkl": 
not a directory: Are you trying to mount a directory onto a file (or vice-versa)?
```

## ✅ Решение

Модель `model.pkl` создается **внутри Docker контейнера** во время сборки, поэтому не нужно монтировать её извне.

### Быстрое исправление на сервере:

```bash
# 1. Остановить контейнеры
docker compose down

# 2. Удалить файл model.pkl если он существует (необязательно)
rm -f model.pkl

# 3. Обновить docker-compose.yml
nano docker-compose.yml
```

**Удалите эту строку:**
```yaml
- ./model.pkl:/app/model.pkl  # ← УДАЛИТЬ ЭТУ СТРОКУ
```

**Также удалите устаревший параметр:**
```yaml
version: '3.8'  # ← УДАЛИТЬ ЭТУ СТРОКУ
```

### Правильный docker-compose.yml:

```yaml
services:
  spam-classifier:
    build: .
    container_name: spam_classifier
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
```

### Запуск после исправления:

```bash
# Пересобрать и запустить
docker compose up --build -d

# Проверить статус
docker compose ps

# Проверить логи
docker compose logs -f

# Проверить что работает
curl http://localhost:5001
```

## 🔍 Почему это произошло?

1. **Dockerfile уже создает модель:**
   ```dockerfile
   RUN python src/prepare.py && python src/train.py
   ```
   
2. **Volume mapping был лишним:**
   - Docker пытался смонтировать host файл `model.pkl`
   - Но файл не существовал на host (создается только в контейнере)
   - Это вызвало конфликт

3. **Правильный подход:**
   - Модель создается при сборке образа
   - Сохраняется внутри контейнера
   - Не требует внешнего монтирования

## 📝 Альтернативные решения

### Вариант 1: Создать модель перед запуском (текущий)
```dockerfile
RUN python src/prepare.py && python src/train.py
```
✅ Модель в контейнере, готова к использованию

### Вариант 2: Volume для переобучения
Если хотите переобучать модель без пересборки:

```yaml
volumes:
  - ./data:/app/data
  - model_data:/app/  # Named volume
```

```bash
# Переобучить модель
docker compose exec spam-classifier python src/train.py
```

### Вариант 3: Bind mount (только если model.pkl уже существует)
```yaml
volumes:
  - ./data:/app/data
  - ./model.pkl:/app/model.pkl:ro  # read-only
```

⚠️ **Требует наличия model.pkl на host перед запуском!**

## 🎯 Рекомендация

**Используйте текущий подход (Вариант 1):**
- Модель создается при сборке
- Контейнер полностью автономный
- Не требует внешних зависимостей
- Проще в деплое

## ✅ Финальная проверка

После исправления проверьте:

```bash
# 1. Контейнер запущен
docker compose ps
# Должен показать: spam_classifier   Up

# 2. Логи без ошибок
docker compose logs spam-classifier
# Должен показать: Running on http://0.0.0.0:5001

# 3. API работает
curl http://localhost:5001
# Должен вернуть HTML страницу

# 4. Предсказание работает
curl -X POST http://localhost:5001/predict \
  -H "Content-Type: application/json" \
  -d '{"message":"Win free iPhone now!"}'
# Должен вернуть: {"prediction":"spam",...}

# 5. Русский язык работает
curl -X POST http://localhost:5001/predict \
  -H "Content-Type: application/json" \
  -d '{"message":"СРОЧНО! Вы выиграли iPhone!"}'
# Должен вернуть: {"prediction":"spam",...}
```

## 🚀 Готово!

Теперь контейнер должен запуститься без ошибок.
