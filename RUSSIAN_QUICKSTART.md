# 🇷🇺 Работа с русскоязычными данными

Быстрое руководство по использованию русскоязычного датасета для обучения модели определения спама.

## ⚡ Быстрый старт

### 1. Проверка данных

Русскоязычные сообщения находятся в файле:
```
data/russian_messages.csv
```

Просмотр содержимого:
```bash
head -20 data/russian_messages.csv
```

### 2. Объединение с основным датасетом

```bash
# Создать многоязычный датасет
python src/merge_russian_data.py

# Заменить raw.csv многоязычной версией
python src/merge_russian_data.py --update-raw
```

### 3. Обучение модели

```bash
# Подготовка данных
python src/prepare.py

# Обучение
python src/train.py
```

### 4. Тестирование

```bash
# Python
python src/predict.py "СРОЧНО! Вы выиграли iPhone!"

# Или через веб-интерфейс
python ui/app.py
```

## 📊 Что включено

### 150 сообщений на русском языке:

✅ **75 легитимных** (ham):
- Личная переписка
- Рабочие сообщения
- Напоминания и уведомления
- Бытовые вопросы

✅ **75 спам** сообщений:
- Фишинг
- Мошеннические схемы
- Поддельные выигрыши
- Подозрительные кредитные предложения
- Запугивания

## 🎯 Примеры распознавания

После обучения модель будет определять:

### ✅ HAM (легитимные)
```
"Привет! Как дела? Когда встретимся?"
"Встреча перенесена на пятницу в 15:00"
"Спасибо за помощь, без тебя бы не справился"
```

### ❌ SPAM (мошенничество)
```
"СРОЧНО! Вы выиграли iPhone 15 Pro!"
"ВНИМАНИЕ! Ваша карта заблокирована!"
"Заработок от 50000 рублей в день!"
```

## 📁 Структура после объединения

```
data/
├── raw.csv                    # ← Многоязычный (EN + RU)
├── raw_english_only.csv       # ← Резервная копия (только EN)
├── russian_messages.csv       # ← Исходные русские данные
├── raw_multilingual.csv       # ← Объединенный файл
├── train.csv                  # ← Для обучения
└── test.csv                   # ← Для тестирования
```

## 🚀 Использование через API

```python
from src.predict import predict_message, predict_proba

# Простое предсказание
result = predict_message("ВНИМАНИЕ! Ваша карта заблокирована!")
print(result)  # spam

# С вероятностями
prob = predict_proba("Привет! Как дела?")
print(prob)  # {'ham': 0.95, 'spam': 0.05}
```

## 💡 Полезные команды

```bash
# Статистика датасета
python -c "
import pandas as pd
df = pd.read_csv('data/russian_messages.csv')
print(f'Всего: {len(df)}')
print(f'Спам: {len(df[df.v1==\"spam\"])}')
print(f'Ham: {len(df[df.v1==\"ham\"])}')
"

# Примеры спама
grep "^spam," data/russian_messages.csv | head -5

# Примеры ham
grep "^ham," data/russian_messages.csv | head -5
```

## 📖 Подробная документация

Полная документация: [docs/russian_dataset.md](docs/russian_dataset.md)

## ⚠️ Важные замечания

1. **Резервная копия**: Оригинальный `raw.csv` сохраняется как `raw_english_only.csv`
2. **Кодировка**: Русские файлы используют UTF-8
3. **Формат**: Совместим с оригинальным датасетом
4. **Конфиденциальность**: Все данные синтетические, реальных номеров нет

## 🔄 Откат изменений

Чтобы вернуться к английскому датасету:
```bash
cp data/raw_english_only.csv data/raw.csv
python src/prepare.py
python src/train.py
```

## 🤝 Добавление своих примеров

1. Откройте `data/russian_messages.csv` в текстовом редакторе
2. Добавьте строки в формате: `ham,"Ваше сообщение",,,` или `spam,"Спам текст",,,`
3. Сохраните в UTF-8
4. Запустите: `python src/merge_russian_data.py --update-raw`
5. Переобучите модель

## 📈 Метрики качества

После обучения проверьте метрики:
```bash
python src/train.py
```

Ожидаемые результаты на многоязычном датасете:
- Accuracy: ~96-98%
- Precision (spam): ~90-95%
- Recall (spam): ~85-90%
- F1-Score: ~0.90-0.95

## 🎓 Следующие шаги

1. ✅ Объедините датасеты
2. ✅ Обучите модель
3. ✅ Протестируйте на своих примерах
4. 📝 Добавьте больше примеров если нужно
5. 🔄 Переобучите для улучшения качества
6. 🚀 Деплойте через Docker

```bash
# Запуск через Docker с новой моделью
docker-compose up --build
```

---

**Вопросы?** Читайте полную документацию в папке `docs/`
